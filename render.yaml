services:
  - type: web
    name: slack-bolt-app
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn app:flask_app --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 120"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: "/opt/render/project/src/.playwright"
